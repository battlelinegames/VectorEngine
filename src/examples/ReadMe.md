# Example Games for AssemblyScript Tutorials

Make sure you compile with the --importMemory flag

For smallest .wasm file size compile with -Oz

```
asc pong.ts --importMemory -Oz -o pong.wasm
```